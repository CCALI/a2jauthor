<ul>
  {{#each templates}}
    <li {{#if isDraggable}}draggable="true"{{/if}} data-index="{{@index}}">
      <templates-list-item template="{.}" is-draggable="{isDraggable}"></templates-list-item>
    </li>
  {{/each}}
</ul>
