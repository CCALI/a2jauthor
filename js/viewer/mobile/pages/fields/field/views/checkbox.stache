<div class="{{#if hasError}}has-error{{/if}}">
  <div class="checkbox">
    <input
      id="{{label}}"
      type="checkbox"
      name="{{name}}"
      ($change)="update"
      ($value)="field._answer.values">
    <label for="{{label}}">{{{eval label}}}</label>
  </div>
</div>
