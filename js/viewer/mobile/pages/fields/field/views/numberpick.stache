<div>
  <div class="form-group {{#if field.hasError}}has-error{{/if}}">
    <label for="{{label}}">{{{ eval(label, repeatVarValue) }}}</label>

    <select id="{{label}}" {($value)}="field._answer.values" ($change)="validateField">
      {{#each numberPickOptions}}
        <option value="{{.}}">{{.}}</option>
      {{/each}}
    </select>
  </div>

  {{> invalid-prompt-tpl }}
<div>
