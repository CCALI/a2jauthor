<div class="a2j-radio-button-group">
  <input type="radio"
    id="{{label}}"
    name="{{name}}"
    ($value)="value"
    ($change)="update"
    class="a2j-radio-button" />

  <label class="a2j-radio-button-label" for="{{label}}">{{{eval label}}}</label>
</div>
