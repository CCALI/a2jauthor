<can-import from="viewer/mobile/pages/fields/field/views/radio.less!" />

<div>
	<div class="a2j-radio-button-group">
		<input
			type="radio"
			id="{{label}}"
			name="{{name}}"
			{($value)}="value"
			{{#if shouldBeChecked}}checked{{/if}}
			class="a2j-radio-button"
			($change)="validateField"/>

		<label class="a2j-radio-button-label" for="{{label}}">{{{ eval(label, repeatVarValue) }}}</label>
	</div>

	{{> invalid-prompt-tpl }}
</div>
